spring:
  datasource:
    url: jdbc:postgresql://localhost:2345/k9x-backend
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver
  jooq:
    sql-dialect: POSTGRES

k9x-backend:
  security:
    refresh-cookie:
      same-site: "Lax"
      secure: false
